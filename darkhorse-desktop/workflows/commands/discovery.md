# Command: Governed Discover

Use the Discovery skill to collect optional input, then invoke the installed Darkhorse CLI `discover` command with the selected generated-project root and unchanged VEP arguments.

Success is exactly the delegated zero exit. Nonzero, missing project-local VEP, stale configuration, or malformed governed output remains fail-closed with no Desktop fallback.
