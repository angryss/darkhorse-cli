# Command: Governed Plan

Use the Planning skill. If A1 has not been materialized, pass one complete optional draft through the approved adapter. If A1 exists, amend canonical `contract.yaml` through the adapter and regenerate proposal/tasks; never edit a projection directly.

Invoke the installed Darkhorse CLI `plan` command. VEP decides readiness and transition truth.
