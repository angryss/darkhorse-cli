# Command: Bounded Implement

Invoke the installed Darkhorse CLI `implement <change-id>` boundary for the selected generated project. Continue only when it verifies the approved A1.

Implement the bounded A1 subject. Treat task/slice labels only as local work tracking. Delegate `test` and `review` through the CLI; do not infer proof, review, completion, or Close.
