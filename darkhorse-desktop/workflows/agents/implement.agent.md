---
description: "Implement only an approved canonical A1 after the governed Darkhorse implementation boundary passes."
tools: [read, search, edit, execute]
---

# Implement agent

Load the Implementation skill. Invoke the installed Darkhorse CLI `implement <change-id>` boundary against the selected generated project before editing code. Implement only the approved A1 scope.

Local work-item status is presentation/tracking data, not VEP proof or completion. Delegate Test and Review through project-local VEP and never archive or Close based on Desktop state.
