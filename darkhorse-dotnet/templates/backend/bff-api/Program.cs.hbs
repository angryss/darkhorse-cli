// {{project.dotnet.namespace}}.Presentation — Entry point (BFF-API archetype)
// Minimal ASP.NET Core host. Expand as your bounded contexts grow.

var builder = WebApplication.CreateBuilder(args);

// TODO: Register MediatR, FluentValidation, AutoMapper from Application layer
// TODO: Register HttpClient factories and MassTransit from Infrastructure layer
// TODO: Configure JWT authentication
// TODO: Register Serilog from Presentation config

builder.Services.AddControllers();
builder.Services.AddEndpointsApiExplorer();
builder.Services.AddSwaggerGen();

var app = builder.Build();

if (app.Environment.IsDevelopment())
{
    app.UseSwagger();
    app.UseSwaggerUI();
}

app.UseAuthentication();
app.UseAuthorization();
app.MapControllers();
app.Run();
