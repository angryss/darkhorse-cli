# {{project.name}}

> {{project.description}}

## Workspace

- **Architecture:** DDD + Onion Architecture + CQRS (per service)
- **Backend:** ASP.NET Core (.NET, per service archetype)
{{#if project.features.frontend}}
- **Frontend:** {{#if (eq project.frontend.platform "both")}}React (Web) + React Native (Mobile){{else if (eq project.frontend.platform "mobile")}}React Native (Expo){{else}}React{{/if}}
{{/if}}
- **Initialized:** {{timestamp}} by DarkHorse .NET CLI v{{cliVersion}}

## Workspace Structure

```
{{project.name}}/
├── context/                   ← AI navigation
├── openspec/                  ← Specs, proposals, workflow
│   ├── AGENTS.md              ← AI agent rules
│   ├── specs/
│   │   ├── project/           ← Roadmap, progress tracker, MVPs
│   │   ├── architecture/      ← Architecture decisions
│   │   ├── domain/            ← Bounded contexts, DDD models
│   │   └── workflow/          ← Development workflow
│   ├── changes/               ← Active proposals
│   └── archive/               ← Completed proposals
├── backend/
│   ├── apis/                  ← REST API services (darkhorse-dotnet add api)
│   ├── bffs/                  ← Backend-for-Frontend services (darkhorse-dotnet add bff-api)
│   └── microservices/         ← Message-driven services (darkhorse-dotnet add microservice)
{{#if project.features.frontend}}
├── frontend/
{{#if (eq project.frontend.platform "both")}}│   ├── web-app/               ← React web application
│   └── mobile-app/            ← React Native mobile app
{{else if (eq project.frontend.platform "mobile")}}│   └── mobile-app/            ← React Native mobile app (Expo)
{{else}}│   └── web-app/               ← React web application
{{/if}}{{/if}}
├── deployment/                ← Docker, Kubernetes
└── .darkhorse.yaml            ← Workspace config
```

## Adding Services

```bash
# Add a REST API service
darkhorse-dotnet add api --name order-api --namespace {{project.name}}.Orders

# Add a Backend-for-Frontend
darkhorse-dotnet add bff-api --name shell-bff --namespace {{project.name}}.ShellBff

# Add a message-driven microservice
darkhorse-dotnet add microservice --name notification-ms --namespace {{project.name}}.Notifications
```

Each service is an isolated .NET solution:
```
backend/<category>/<service-name>/
├── <Namespace>.sln
├── src/
│   ├── <Namespace>.Presentation/     ← Controllers / Consumers
│   ├── <Namespace>.Application/      ← CQRS handlers, use cases
│   ├── <Namespace>.Domain/           ← Entities, value objects, events
│   └── <Namespace>.Infrastructure/   ← Repos, DB, messaging clients
└── tests/
    ├── <Namespace>.UnitTests/
    └── <Namespace>.IntegrationTests/
```

{{#if project.features.frontend}}
## Frontend

```bash
{{#if (eq project.frontend.platform "both")}}
# Start web app
cd frontend/web-app && npm install && npm run dev

# Start mobile app (separate terminal)
cd frontend/mobile-app && npm install && npx expo start
{{else if (eq project.frontend.platform "mobile")}}
cd frontend/mobile-app && npm install && npx expo start
{{else}}
cd frontend/web-app && npm install && npm run dev
{{/if}}
```

{{/if}}
## For AI Agents

Start with `context/00-START-HERE.md`, then read `openspec/AGENTS.md`.

All AI tools share the same OpenSpec source of truth.

## Development Workflow
{{#if project.ai.tools.kiro}}
### Kiro

Use natural-language instructions referencing the workflow phases:

| Phase | Instruction | Output |
|---|---|---|
| Discover | *"Run the Discover workflow for [feature]"* | `openspec/changes/discoveries/DISC-###.md` |
| Plan | *"Plan [feature] based on DISC-###"* | `openspec/changes/<mvp>/proposal.md` |
| Implement | *"Implement proposal from [mvp path]"* | Source code + updated tracker |
| Troubleshoot | *"Troubleshoot [issue]"* | `openspec/changes/bugs/BUG-###.md` |

Kiro reads steering context from `.kiro/steering/` and uses prompts in `.kiro/prompts/`.
{{/if}}
{{#if project.ai.tools.copilot}}
### GitHub Copilot

Slash commands in `.github/prompts/`:

- `/discover` — explore and shape ideas
- `/plan` — create proposals
- `/implement` — execute proposals inside-out
- `/troubleshoot` — diagnose issues
{{/if}}

### Workflow Phases

1. **Discover** — shape the idea, write `DISC-###.md` in `openspec/changes/discoveries/`
2. **Plan** — design the solution, write `proposal.md` + `tasks.md` in `openspec/changes/<mvp>/`
3. **Implement** — execute inside-out: Domain → Application → Infrastructure → Presentation
4. **Test** — run tests, file bugs as `BUG-###.md` in `openspec/changes/bugs/`
5. **Archive** — move completed MVPs to `openspec/archive/`

See `openspec/specs/workflow/` for detailed playbooks.

