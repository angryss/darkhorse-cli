<!-- Test.Ms.Infrastructure — Persistence + messaging (Microservice archetype) -->
<!-- Microservice owns its data and communicates via message broker -->
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>Test.Ms.Infrastructure</RootNamespace>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../Test.Ms.Application/Test.Ms.Application.csproj" />
    <ProjectReference Include="../Test.Ms.Domain/Test.Ms.Domain.csproj" />
    <ProjectReference Include="../Test.Ms.Common/Test.Ms.Common.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Persistence (microservice owns its data) -->
    <PackageReference Include="Microsoft.EntityFrameworkCore" Version="8.0.*" />
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="8.0.*" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Tools" Version="8.0.*">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers</IncludeAssets>
    </PackageReference>

    <!-- Messaging (message-driven, primary interface) -->
    <PackageReference Include="MassTransit" Version="8.2.*" />
    <PackageReference Include="MassTransit.RabbitMQ" Version="8.2.*" />

    <!-- NOTE: No REST client dependency. Microservice does not call downstream APIs. -->
  </ItemGroup>

</Project>
