<!-- DarkHorse.Mobile.Presentation — Message listeners, health endpoints (Microservice archetype) -->
<!-- Microservice is NOT HTTP-first. Presentation = message listeners + optional health. -->
<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>DarkHorse.Mobile.Presentation</RootNamespace>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../DarkHorse.Mobile.Application/DarkHorse.Mobile.Application.csproj" />
    <ProjectReference Include="../DarkHorse.Mobile.Infrastructure/DarkHorse.Mobile.Infrastructure.csproj" />
    <ProjectReference Include="../DarkHorse.Mobile.Common/DarkHorse.Mobile.Common.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Health checks only — microservice is not HTTP-first -->
    <PackageReference Include="AspNetCore.HealthChecks.NpgSql" Version="8.0.*" />
    <PackageReference Include="Serilog.AspNetCore" Version="8.0.*" />
    <!-- NOTE: No Swagger/OpenAPI by default. Add only if exposing admin endpoints. -->
  </ItemGroup>

</Project>
