import { type DiscoveryInput, type DiscoveryOutput } from '../skills/discovery.js';
/**
 * Discover agent — orchestrates the product discovery workflow.
 * Thin orchestrator; real logic lives in the discovery skill.
 */
export declare function discoverAgent(input: DiscoveryInput): Promise<DiscoveryOutput>;
//# sourceMappingURL=discover.agent.d.ts.map