import { logger } from '../core/logger.js';

/**
 * Plan agent — orchestrates the planning workflow.
 * Thin orchestrator; real logic lives in skills.
 */
export async function planAgent(): Promise<void> {
  logger.warn('Plan agent not yet implemented');
}
