# {{project.name}} — Project Overview

> **AI Steering File** — Read by Kiro on every session start.
> This file is generated by DarkHorse. Do not duplicate content here — use it to point Kiro at canonical sources.

## Source of Truth

All workflow definitions, architecture rules, and AI agent instructions live in **OpenSpec**:

| Resource | Location | Purpose |
|---|---|---|
| AI entry point | `openspec/AGENTS.md` | Start here — full project context and agent roster |
| Architecture rules | `openspec/specs/architecture/` | Onion Architecture, CQRS, WPF constraints |
| Pattern guides | `openspec/specs/patterns/` | Implementation patterns and coding guides |
| Workflow skills | `openspec/specs/workflow/skills/` | Step-by-step execution playbooks |
| Domain model | `openspec/specs/domain/` | Bounded contexts and aggregate definitions |
| Active changes | `openspec/changes/` | In-flight proposals and MVPs |

## Project Identity

| Field | Value |
|---|---|
| Name | {{project.name}} |
| Description | {{project.description}} |
| Namespace | `{{project.dotnet.namespace}}` |
| .NET Version | {{project.dotnet.dotnetVersion}} |
| UI Framework | {{project.dotnet.uiFramework}} |
| Version | {{project.version}} |

## Solution Structure

```
{{project.name}}/
├── src/
│   ├── {{project.dotnet.namespace}}.Common/        # primitives, contracts — no deps
│   ├── {{project.dotnet.namespace}}.Domain/        # entities, aggregates, domain events
│   ├── {{project.dotnet.namespace}}.Application/   # CQRS commands, queries, handlers
│   ├── {{project.dotnet.namespace}}.Infrastructure/ # persistence, adapters
│   └── {{project.dotnet.namespace}}.Presentation/  # WPF app — entry point
├── tests/
│   ├── {{project.dotnet.namespace}}.UnitTests/
│   └── {{project.dotnet.namespace}}.IntegrationTests/
├── context/
└── openspec/
```

## How to Use This Project with Kiro

1. Read `openspec/AGENTS.md` first — it is the canonical AI entry point.
2. Use the context files in `context/` for workspace orientation.
3. Follow the workflow defined in `openspec/specs/workflow/skills/` for all changes.
4. All proposals and specs go into `openspec/changes/` before any code is written.

## Do Not

- Modify OpenSpec content directly — use the workflow agents defined in `openspec/AGENTS.md`.
- Create Kiro-specific specs — OpenSpec is the single source of truth.
- Bypass the proposal gate — no code changes without an approved spec in `openspec/changes/`.
- Write specs, requirements, or task files to `.kiro/specs/` — use `openspec/changes/` exclusively.
