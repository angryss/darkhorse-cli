// {{project.name}} — Frontend entry point
// Tauri IPC is available via @tauri-apps/api

const app = document.getElementById("app");
if (app) {
  app.innerHTML = `
    <h1>{{project.name}}</h1>
    <p>{{project.description}}</p>
  `;
}
